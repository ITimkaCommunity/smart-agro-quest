# 🚀 Быстрый старт проекта EduFarm

## Предварительные требования

- **Docker** и **Docker Compose** установлены и запущены
- Проверьте установку:
  ```bash
  docker --version
  docker-compose --version
  ```

## Запуск проекта

### 1. Перейдите в директорию проекта
```bash
cd smart-agro-quest-main
```

### 2. Запустите все сервисы одной командой
```bash
docker-compose up -d
```

Эта команда:
- Создаст и запустит все контейнеры в фоновом режиме (`-d`)
- Поднимет PostgreSQL, Redis, MinIO, Backend, Frontend и Nginx
- Автоматически применит миграции базы данных

### 3. Проверьте статус контейнеров
```bash
docker-compose ps
```

Или:
```bash
docker ps
```

### 4. Просмотр логов (если нужно)
```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только frontend
docker-compose logs -f frontend
```

## Остановка проекта

### Остановить все контейнеры (данные сохраняются)
```bash
docker-compose stop
```

### Остановить и удалить контейнеры (данные сохраняются)
```bash
docker-compose down
```

### Полная очистка (удалить контейнеры, сети и volumes с данными)
```bash
docker-compose down -v
```

⚠️ **Внимание**: Команда `down -v` удалит все данные из базы данных!

## Перезапуск проекта

### Перезапустить все сервисы
```bash
docker-compose restart
```

### Перезапустить конкретный сервис
```bash
docker-compose restart backend
docker-compose restart frontend
```

## Пересборка после изменений в коде

### Пересобрать и перезапустить backend
```bash
docker-compose build backend
docker-compose up -d backend
```

### Пересобрать и перезапустить frontend
```bash
docker-compose build frontend
docker-compose up -d frontend
```

### Пересобрать все сервисы
```bash
docker-compose build
docker-compose up -d
```

## Адреса для работы

После запуска проект будет доступен по адресам:

- **Frontend (UI)**: http://localhost:5173
- **Frontend через Nginx**: http://localhost
- **Backend API**: http://localhost:3001
- **Swagger документация**: http://localhost:3001/api
- **MinIO Console**: http://localhost:9001 (логин: `minioadmin`, пароль: `minioadmin123`)

## Полезные команды

### Просмотр логов конкретного сервиса
```bash
docker logs edufarm-backend -f
docker logs edufarm-frontend -f
docker logs edufarm-postgres -f
```

### Выполнение команд внутри контейнера
```bash
# Зайти в контейнер backend
docker exec -it edufarm-backend sh

# Выполнить команду в контейнере
docker exec edufarm-backend npm run seed  # Заполнить БД тестовыми данными
```

### Проверка здоровья сервисов
```bash
# Проверить backend
curl http://localhost:3001/health

# Проверить frontend
curl http://localhost:5173
```

## Решение проблем

### Если контейнеры не запускаются

1. **Проверьте логи**:
   ```bash
   docker-compose logs
   ```

2. **Проверьте, не заняты ли порты**:
   ```bash
   # Windows PowerShell
   netstat -ano | findstr :3001
   netstat -ano | findstr :5173
   ```

3. **Очистите и пересоздайте все**:
   ```bash
   docker-compose down -v
   docker-compose up -d --build
   ```

### Если база данных не работает

1. **Проверьте логи PostgreSQL**:
   ```bash
   docker logs edufarm-postgres
   ```

2. **Пересоздайте базу данных**:
   ```bash
   docker-compose down -v
   docker-compose up -d
   ```

### Если нужно сбросить базу данных

```bash
# Остановить контейнеры
docker-compose down -v

# Запустить заново
docker-compose up -d
```

## Заполнение базы данных тестовыми данными

После первого запуска можно заполнить БД тестовыми данными:

```bash
docker exec edufarm-backend npm run seed
```

Это создаст:
- 5 образовательных зон
- Предметы фермы
- 25+ достижений
- 18+ обучающих заданий

## Структура команд

### Основные команды для ежедневной работы:

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose stop

# Просмотр логов
docker-compose logs -f backend

# Перезапуск
docker-compose restart

# Полная очистка (когда что-то сломалось)
docker-compose down -v
docker-compose up -d
```

## Примечания

- При первом запуске Docker скачает все необходимые образы (может занять время)
- Миграции базы данных применяются автоматически при запуске backend
- Все данные сохраняются в Docker volumes и не удаляются при `docker-compose down` (без `-v`)
- Для полной очистки используйте `docker-compose down -v`

