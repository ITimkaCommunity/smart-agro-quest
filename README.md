# EduFarm - Образовательная платформа с геймификацией

**EduFarm** — это инновационная образовательная платформа, объединяющая обучение с элементами игровой механики фермерского симулятора и виртуального питомца (Тамагочи). Платформа создана для мотивации учеников через систему достижений, прогресса и интерактивных заданий по различным предметам.

## 📋 Содержание

- [Архитектура проекта](#архитектура-проекта)
- [Технологии](#технологии)
- [Установка и запуск](#установка-и-запуск)
- [Структура проекта](#структура-проекта)
- [Основные функции](#основные-функции)
- [API документация](#api-документация)
- [Развертывание](#развертывание)
- [Тестирование](#тестирование)
- [Интеграция с AI](#интеграция-с-ai)

---

## 🏗️ Архитектура проекта

Проект построен на микросервисной архитектуре:

```
┌─────────────────┐      ┌──────────────────┐      ┌─────────────────┐
│   Frontend      │◄────►│   NestJS Backend │◄────►│   PostgreSQL    │
│   (React +      │      │   (REST + WS)    │      │   Database      │
│   Vite)         │      └──────────────────┘      └─────────────────┘
└─────────────────┘              │                           │
        │                        │                           │
        │                        ▼                           ▼
        │              ┌──────────────────┐       ┌─────────────────┐
        │              │   Redis Cache    │       │   MinIO (S3)    │
        │              │   & WebSocket    │       │   File Storage  │
        │              └──────────────────┘       └─────────────────┘
        │
        ▼
┌─────────────────┐
│   FastAPI       │
│   AI Copilot    │
│   (Python)      │
└─────────────────┘
```

## 🚀 Технологии

### Frontend Dependencies
```json
{
  "react": "^18.3.1",
  "react-router-dom": "^6.30.1",
  "tailwindcss": "^3.4.0",
  "@tanstack/react-query": "^5.83.0",
  "socket.io-client": "^4.8.1",
  "@supabase/supabase-js": "^2.75.0",
  "lucide-react": "^0.462.0",
  "zod": "^3.25.76"
}
```

### Backend Dependencies
```json
{
  "@nestjs/core": "^10.0.0",
  "@nestjs/typeorm": "^10.0.0",
  "@nestjs/swagger": "^7.0.0",
  "@nestjs/throttler": "^5.0.0",
  "typeorm": "^0.3.0",
  "socket.io": "^4.8.0",
  "passport-jwt": "^4.0.0",
  "bcrypt": "^5.1.0"
}
```

## 📦 Установка и запуск

### Предварительные требования
- Node.js >= 18.x
- PostgreSQL >= 14.x
- npm или yarn

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd edufarm
```

### 2. Установка зависимостей

#### Frontend
```bash
npm install
```

#### Backend
```bash
cd backend
npm install
```

### 3. Настройка окружения

#### Frontend (.env)
```env
VITE_BACKEND_URL=http://localhost:3001
```

#### Backend (backend/.env)
```env
# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password
DB_DATABASE=edufarm

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# CORS
CORS_ORIGIN=http://localhost:5173

# Server
PORT=3001
NODE_ENV=development
```

### 4. Настройка базы данных

1. Создайте базу данных PostgreSQL:
```sql
CREATE DATABASE edufarm;
```

2. Примените миграции (через Supabase SQL Editor или локально):
```bash
# Используйте SQL файл из backend/database/schema.sql
```

### 4. Заполнение базы данных тестовыми данными

```bash
cd backend
npm run seed
```

Будут созданы:
- 5 образовательных зон (Биология, Химия, Физика, Математика, IT)
- Предметы фермы (семена, урожай, продукты животных)
- 25+ достижений разной редкости
- 18+ обучающих заданий

### 5. Запуск проектов

#### Frontend (Development)
```bash
npm run dev
# Откроется на http://localhost:5173
```

#### Backend (Development)
```bash
cd backend
npm run start:dev
# API доступен на http://localhost:3001
# Swagger документация: http://localhost:3001/api
```

### 6. Тестирование

#### Запуск тестов
```bash
cd backend

# Unit тесты
npm test                # Запустить все unit тесты
npm run test:watch      # Запустить в watch режиме
npm run test:cov        # Запустить с покрытием кода

# E2E тесты
npm run test:e2e        # Запустить end-to-end тесты
```

#### Тестирование через Swagger UI
1. Запустите backend: `npm run start:dev`
2. Откройте http://localhost:3001/api
3. Зарегистрируйтесь через `/auth/signup`
4. Скопируйте `access_token` из ответа
5. Нажмите "Authorize" и вставьте токен
6. Тестируйте любые защищенные эндпоинты

#### Тестирование WebSocket событий
См. подробное руководство в `backend/test/README.md`

Основные WebSocket namespaces:
- `/farm` - События фермы (растения, животные, производство)
- `/pet` - События питомца (кормление, игра, статусы)

## 📚 API Документация

### Swagger UI
После запуска backend сервера, откройте:
**http://localhost:3001/api**

### Основные эндпоинты

#### 🔐 Authentication (`/auth`)
```
POST /auth/signup       - Регистрация нового пользователя
POST /auth/login        - Вход в систему
```

#### 👤 Users (`/users`)
```
GET  /users/profile     - Получить профиль текущего пользователя
PUT  /users/profile     - Обновить профиль
GET  /users/:id         - Получить пользователя по ID
```

#### 📝 Tasks (`/tasks`)
```
GET    /tasks                    - Получить все задания
POST   /tasks                    - Создать задание (teacher/admin)
GET    /tasks/:id                - Получить задание по ID
PATCH  /tasks/:id                - Обновить задание
DELETE /tasks/:id                - Удалить задание
POST   /tasks/:id/submit         - Отправить решение
GET    /tasks/:id/submissions    - Получить решения по заданию
GET    /tasks/user/submissions   - Получить решения пользователя
PATCH  /tasks/submissions/:id/grade - Оценить решение
```

#### 🌾 Farm (`/farm`)
```
# Inventory
GET  /farm/inventory             - Инвентарь пользователя
GET  /farm/items                 - Все фермерские предметы

# Plants
GET   /farm/plants?zoneId=...   - Растения в зоне
POST  /farm/plants               - Посадить семя
POST  /farm/plants/:id/water    - Полить растение
POST  /farm/plants/:id/harvest  - Собрать урожай

# Animals
GET   /farm/animals              - Доступные животные
GET   /farm/animals/user         - Животные пользователя
POST  /farm/animals/:id          - Добавить животное
POST  /farm/animals/user/:id/feed     - Покормить
POST  /farm/animals/user/:id/collect  - Собрать продукцию

# Production
GET   /farm/production/chains?zoneId=...  - Цепочки производства
GET   /farm/production/user?zoneId=...   - Производства пользователя
POST  /farm/production                    - Начать производство
POST  /farm/production/:id/collect       - Собрать продукт
```

#### 🐾 Pet (`/pet`)
```
GET   /pet              - Получить питомца пользователя
POST  /pet              - Создать питомца
POST  /pet/feed         - Покормить питомца
POST  /pet/water        - Напоить питомца
POST  /pet/play         - Поиграть с питомцем
POST  /pet/use-item     - Использовать предмет
GET   /pet/shop         - Магазин предметов
GET   /pet/items        - Предметы пользователя
```

#### 🏆 Achievements (`/achievements`)
```
GET  /achievements       - Все достижения
GET  /achievements/user  - Достижения пользователя
```

#### 📊 Progress (`/progress`)
```
GET  /progress/user      - Прогресс во всех зонах
GET  /progress/:zoneId   - Прогресс в конкретной зоне
```

#### 🎯 Zones (`/zones`)
```
GET  /zones      - Все зоны (предметы)
GET  /zones/:id  - Зона по ID
```

### Примеры запросов

#### Регистрация
```bash
curl -X POST http://localhost:3001/auth/signup \
  -H "Content-Type: application/json" \
  -d '{
    "email": "student@example.com",
    "password": "SecurePass123!",
    "fullName": "Иван Иванов"
  }'
```

#### Создание задания (требуется токен)
```bash
curl -X POST http://localhost:3001/tasks \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Законы Ньютона",
    "description": "Проведи эксперимент",
    "zoneId": "zone-uuid",
    "difficulty": 3,
    "experienceReward": 150,
    "requiredLevel": 2
  }'
```

## 📈 Система прогресса

### Уровни и опыт (XP)
- **1000 XP = 1 уровень**
- XP начисляется за:
  - Выполнение заданий (по заданию индивидуально)
  - Сбор урожая на ферме
  - Производство предметов

### Прогресс по зонам
Каждая зона (предмет) имеет собственный прогресс:
- **Level** - уровень в зоне
- **Experience** - накопленный опыт
- **Tasks Completed** - выполнено заданий
- **Is Unlocked** - доступ разблокирован

### Автоматическое начисление
При оценке задания учителем:
- ✅ Оценка >= 3 → начисляется XP
- ✅ Увеличивается счётчик выполненных заданий
- ✅ Проверяются достижения
- ✅ Обновляется уровень

### Достижения
Автоматическая разблокировка при:
- **level_reached**: Достижение определённого уровня
- **xp_earned**: Накопление определённого количества XP
- **tasks_completed**: Выполнение N заданий
- **perfect_grade**: Получение оценки 5

## 🔌 WebSocket события

### Подключение
```typescript
import io from 'socket.io-client';

const farmSocket = io('http://localhost:3001/farm', {
  auth: { token: 'your-jwt-token' }
});

farmSocket.emit('joinUserRoom', userId);
```

### События Farm (`/farm` namespace)
```typescript
// Растения
'plant:updated'       - Растение обновлено
'plant:harvested'     - Растение собрано
{ plantId, item }

// Животные
'animal:updated'      - Животное обновлено
'animal:collected'    - Продукция собрана
{ animalId, item }

// Производство
'production:started'  - Производство запущено
'production:completed' - Производство завершено
{ productionId, item }

// Инвентарь
'inventory:updated'   - Инвентарь обновлён
```

### События Pet (`/pet` namespace)
```typescript
'pet:created'       - Питомец создан
'pet:statsUpdated'  - Статистики обновлены
'pet:fed'           - Покормлен
'pet:watered'       - Напоён
'pet:played'        - Поиграли
'pet:itemUsed'      - Использован предмет
'pet:ranAway'       - Питомец сбежал
{ petId }
```

### Переподключение
Клиент автоматически переподключается с экспоненциальной задержкой:
- Попытка 1: 1 секунда
- Попытка 2: 2 секунды
- Попытка 3: 4 секунды
- Попытка 4: 8 секунд
- Попытка 5: 16 секунд

## 🗄️ База данных

### Основные таблицы

#### Пользователи
- `profiles` - Профили пользователей
- `user_roles` - Роли (student, teacher, admin)
- `user_zone_progress` - Прогресс по зонам

#### Задания
- `tasks` - Учебные задания
- `task_submissions` - Решения заданий

#### Ферма
- `farm_zones` - Зоны (предметы)
- `farm_items` - Предметы фермы
- `farm_animals` - Типы животных
- `user_inventory` - Инвентарь
- `user_plants` - Посаженные растения
- `user_farm_animals` - Животные пользователя
- `production_chains` - Цепочки производства
- `production_chain_ingredients` - Ингредиенты
- `user_productions` - Активные производства
- `user_farm_slots` - Слоты на ферме

#### Питомец
- `pets` - Виртуальные питомцы
- `pet_shop_items` - Предметы магазина
- `pet_shop_item_costs` - Стоимость предметов
- `user_pet_items` - Предметы пользователя

#### Достижения
- `achievements` - Доступные достижения
- `user_achievements` - Разблокированные достижения

#### Бустеры
- `zone_boosters` - Бустеры для зон
- `user_active_boosters` - Активные бустеры

### RLS Policies
Все таблицы защищены Row Level Security:
- Пользователи видят только свои данные
- Учителя видят задания и решения
- Админы имеют полный доступ

## 🔒 Безопасность

### Реализованные меры
- ✅ JWT аутентификация
- ✅ Rate limiting (100 req/min)
- ✅ Input validation (class-validator)
- ✅ CORS защита
- ✅ Row Level Security (RLS)
- ✅ Password hashing (bcrypt)
- ✅ SQL injection protection (TypeORM)
- ✅ XSS protection (sanitization)

### Рекомендации для production
1. Измените JWT_SECRET
2. Настройте HTTPS
3. Используйте переменные окружения
4. Настройте firewall
5. Регулярно обновляйте зависимости
6. Настройте мониторинг и логирование

## 🧪 Тестирование

```bash
# Frontend tests
npm run test

# Backend tests
cd backend
npm run test
npm run test:e2e
```

## 📝 Лицензия

MIT License - свободное использование

## 👥 Авторы

EduFarm Development Team

## 🤝 Вклад в проект

Приветствуются Pull Requests! Для крупных изменений сначала откройте Issue.

---

## Lovable Project Info

**URL**: https://lovable.dev/projects/13b98043-3987-4a5f-af82-f403e9f12445

**Документация обновлена**: 2025-11-10
