# 🔗 Подключение проекта к GitHub репозиторию

## Шаг 1: Найдите URL вашего репозитория на GitHub

1. Откройте ваш репозиторий на GitHub
2. Нажмите кнопку зеленую кнопку **"Code"**
3. Скопируйте URL (HTTPS или SSH)

Примеры:
- HTTPS: `https://github.com/ваш-username/smart-agro-quest.git`
- SSH: `git@github.com:ваш-username/smart-agro-quest.git`

## Шаг 2: Инициализация Git репозитория

```bash
# Инициализировать git репозиторий
git init

# Добавить удаленный репозиторий (замените URL на ваш!)
git remote add origin https://github.com/ваш-username/smart-agro-quest.git

# Проверить, что remote добавлен
git remote -v
```

## Шаг 3: Добавить все файлы и сделать первый commit

```bash
# Добавить все файлы
git add .

# Сделать commit с вашими изменениями
git commit -m "Fix: исправлена миграция базы данных и health check

- Добавлены проверки существования таблиц в миграции
- Исправлен health check (таблица users вместо user)
- Исправлен порт frontend (5173 вместо 4173)
- Отключен конфликтующий schema.sql"

# Проверить статус
git status
```

## Шаг 4: Получить изменения с GitHub (если они есть)

```bash
# Получить информацию о ветках
git fetch origin

# Если на GitHub есть ветка main или master, нужно сначала получить её
git pull origin main --allow-unrelated-histories
# или
git pull origin master --allow-unrelated-histories
```

## Шаг 5: Отправить изменения на GitHub

### Вариант A: Если это ваш репозиторий (вы владелец)

```bash
# Отправить изменения в ветку main
git push -u origin main

# Если ветка называется master
git push -u origin master
```

### Вариант B: Если это форк или вы хотите создать новую ветку

```bash
# Создать новую ветку для ваших изменений
git checkout -b fix/migration-and-health-check

# Отправить новую ветку
git push -u origin fix/migration-and-health-check
```

## Шаг 6: Создать Pull Request (если нужно)

1. Откройте ваш репозиторий на GitHub
2. Нажмите **"Compare & pull request"**
3. Заполните описание изменений
4. Нажмите **"Create pull request"**

## Полезные команды

```bash
# Посмотреть изменения
git status

# Посмотреть что изменилось
git diff

# Посмотреть историю коммитов
git log --oneline

# Отменить изменения в файле (если нужно)
git checkout -- имя-файла

# Посмотреть какие файлы будут добавлены
git status --short
```

## Если возникли конфликты

Если при `git pull` возникли конфликты:

```bash
# Посмотреть конфликты
git status

# Решить конфликты вручную в файлах
# Затем:
git add .
git commit -m "Resolve merge conflicts"
git push
```

## Быстрая команда (если знаете URL репозитория)

Замените `YOUR_REPO_URL` на URL вашего репозитория:

```bash
git init
git remote add origin YOUR_REPO_URL
git add .
git commit -m "Fix: исправлена миграция и health check"
git branch -M main
git push -u origin main
```

## Важно!

⚠️ **Если вы не владелец репозитория:**
- Создайте **Fork** репозитория на GitHub
- Используйте URL вашего форка
- Создайте новую ветку для изменений
- Отправьте Pull Request в оригинальный репозиторий

⚠️ **Если репозиторий приватный:**
- Убедитесь, что у вас есть доступ
- Возможно, потребуется авторизация через GitHub CLI или SSH ключи

